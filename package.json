{
  "name": "gym-tracker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
